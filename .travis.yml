sudo: required

language: python 

python: 
  - "2.7"

services:
  - docker

before_install:
  - cd docker/python2
  - docker build -t sonata .
  - docker run sonata /bin/bash -c "/home/docker/env/neurosci/bin/python2 versioninfo.py"

script:
  - pwd
  - ls -alt
  - docker images
